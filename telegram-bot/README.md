Run the bot with `MCP_SERVER_URL` pointing to the deployed Wema API MCP endpoint:

```bash
python3 bot.py
```

The endpoint should end with `/mcp`, for example `https://your-service.run.app/mcp`.

Then go to telegram and find the bot @bankin_with_alat_bot
