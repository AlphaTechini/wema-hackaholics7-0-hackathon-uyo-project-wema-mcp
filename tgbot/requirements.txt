# Telegram bot
python-telegram-bot==21.6

# AI provider clients
openai==1.40.0
groq==1.5.0

# FastAPI stack (ALAT mock API + MCP server)
fastapi==0.111.1
uvicorn[standard]==0.30.3

# HTTP client (used inside the bot and MCP server)
httpx==0.27.0

# Pydantic (FastAPI uses this; pin to v2)
pydantic==2.8.2

# Environment variables
python-dotenv==1.0.1
